#################################################
#### Custom function 8 (at least 2 required) ####
#################################################

lenique <- function(x) {
  # calculate the length of unique values
  length(unique(x))
}
